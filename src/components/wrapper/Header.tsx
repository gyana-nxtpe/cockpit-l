import React from 'react'

const Header = ({
    children
}:{
    children: React.ReactNode
}) => {
  return (
    <div>
        {/* Navbar */}

        {/* SidePannel */}

        {children}
    </div>
  )
}

export default Header